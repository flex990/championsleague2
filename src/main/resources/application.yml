server:
    port: 8080

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/cl2?serverTimezone=Europe/Rome
        username: root
        password: password
        driver: com.mysql.cj.jdbc.Driver
    liquibase:
        change-log: classpath:/db/changelog.xml

logging:
    level:
        root: INFO



